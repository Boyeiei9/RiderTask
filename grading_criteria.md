# เอกสารอ้างอิงเกณฑ์การให้คะแนน (Grading Criteria Reference)

เอกสารนี้รวบรวมข้อมูลและตำแหน่งของโค้ดในโปรเจกต์ (RiderTask) ที่สอดคล้องกับเกณฑ์การให้คะแนนแต่ละข้อ พร้อมคำอธิบายโดยละเอียดครับ

---

## 1. `nav` (Navigation - การนำทางในแอป)
โปรเจกต์นี้ใช้ **Expo Router** ซึ่งเป็นระบบ File-based routing สำหรับจัดการหน้าตาและการเปลี่ยนหน้าของแอปพลิเคชัน
* **ตำแหน่งในโค้ด:**
  * โครงสร้างโฟลเดอร์ใน `app/` เช่น `app/(rider)/_layout.tsx` และ `app/(sender)/_layout.tsx` ที่มีการใช้ `<Tabs>` สำหรับทำ Bottom Tab Navigation เพื่อสลับหน้าไปมา เช่น หน้าแรก (Home), ประวัติ (History), โปรไฟล์ (Profile)
  * การใช้ `router.push(...)` หรือคอมโพเนนต์ `<Link>` ที่นำเข้ามาจาก `expo-router` เพื่อเปลี่ยนหน้าจอ ตัวอย่างเช่น การกดเข้าไปดูรายละเอียดออเดอร์ หรือกดไปหน้าแชท

---

## 2. `home` (หน้าหลักของแอป)
หน้าหลักที่ทำหน้าที่เป็นจุดเริ่มต้นหรือ Dashboard ให้ผู้ใช้หลังจากล็อกอินเข้ามา
* **ตำแหน่งในโค้ด:**
  * **ฝั่งคนส่ง (Sender):** ไฟล์ `app/(sender)/index.tsx` เป็นหน้าโฮมของคนส่งของ ซึ่งจะมีแผนที่ให้ดู และมีปุ่มสำหรับสร้างออเดอร์ใหม่
  * **ฝั่งไรเดอร์ (Rider):** ไฟล์ `app/(rider)/index.tsx` เป็นหน้าโฮมของไรเดอร์ ซึ่งจะแสดงรายการงาน (Tasks) ที่สามารถกดรับได้ และแสดงสถานะเปิด/ปิดรับงานของตัวเอง

---

## 3. `flatlist` (การแสดงผลรายการข้อมูลจำนวนมาก)
มีการใช้คอมโพเนนต์ `<FlatList>` จาก `react-native` สำหรับแสดงผลข้อมูลที่เป็น Array (ลิสต์) คอยจัดการการเรนเดอร์ข้อมูลเยอะๆ ให้ลื่นไหล
* **ตำแหน่งในโค้ด:**
  * หน้ารายการออเดอร์ `app/(sender)/orders.tsx`
  * หน้าประวัติการส่ง `app/(rider)/history.tsx`
* **คำอธิบาย:**
  ในการใช้ FlatList ของโปรเจกต์นี้ มีการตั้งค่าส่วนประกอบสำคัญครบถ้วน ได้แก่:
  * `data={...}` (ใส่ข้อมูลอาเรย์ของออเดอร์ลงไป)
  * `keyExtractor={...}` (ระบุค่า ID ไม่ให้ซ้ำกัน)
  * `renderItem={...}` (เรียกใช้คอมโพเนนต์ `<OrderCard />` สำหรับเรนเดอร์แต่ละรายการ)
  * `refreshControl={...}` (ระบบดึงหน้าจอลงมาเพื่อโหลดข้อมูลใหม่ หรือ Pull-to-refresh)
  * `ListEmptyComponent={...}` (หน้าจอแสดงผลเวลาที่ไม่มีข้อมูลในรายการ เช่น "พร้อมส่งพัสดุชิ้นแรกหรือยัง?")

---

## 4. `fetch` (การดึงข้อมูลผ่านเครือข่าย)
การดึงข้อมูล หรือการโหลดเนื้อหามาจากอินเทอร์เน็ต รูปแบบปกติและสำหรับแปลงไฟล์
* **ตำแหน่งในโค้ด:**
  * ไฟล์ `services/storageService.ts` ในฟังก์ชัน `uploadImageAsync`
* **คำอธิบาย:**
  โปรเจกต์นี้ใช้คำสั่ง `fetch()` มาตรฐานของ JavaScript ควบคู่ไปกับเครื่องมือของ Firebase โดยมีการใช้ `await fetch(uri)` ในกรณีที่จะอ่านไฟล์รูปภาพที่อยู่ในเครื่อง (ในรูปแบบของ Local URI) และแปลงผลลัพธ์ที่ได้ออกมาเป็น Blob (`await response.blob()`) ก่อนอัปโหลดส่งไปยัง Server (Firebase Storage) นอกจากนี้ฟังก์ชันการดึงข้อมูลส่วนใหญ่จะใช้ SDK ของ Firebase เอง (เช่น `getDocs`, `onSnapshot`) แทนการยิง REST API แบบเดิม

---

## 5. `Async` (Asynchronous Functions และ AsyncStorage)
การทำงานแบบไม่ปิดกั้น (Asynchronous) และการบันทึกข้อมูลถาวรไว้ที่เครื่องผู้ใช้ (AsyncStorage)
* **ตำแหน่งในโค้ด:**
  * **Asynchronous (async/await):** มีการใช้ `async` ควบคู่กับ `await` ทุกจุดที่ต้องรอผลลัพธ์ เช่น ฟังก์ชัน `fetchOrders = async () => { ... }` หรือการบันทึกข้อมูลลงฐานข้อมูล
  * **AsyncStorage (สำหรับเซฟข้อมูลที่เครื่อง):** ถูกใช้งานในไฟล์ `services/authService.ts`
* **คำอธิบาย:**
  * ส่วนของ **AsyncStorage** ใช้สำหรับเก็บสถานะล็อกอินของผู้ใช้ (`userId` หรือ `userRole`) ไว้ในเครื่องโทรศัพท์ เพื่อให้เวลาปิดแอปแล้วเปิดใหม่ผู้ใช้ก็ยังคงล็อกอินค้างไว้อยู่เสมอ ไม่ต้องล็อกอินใหม่ทุกครั้ง

---

## 6. `map` (แผนที่)
การแสดงผลแผนที่ (Maps) และจุดระบุตำแหน่ง (Markers) ในแอปพลิเคชัน
* **ตำแหน่งในโค้ด:**
  * ใช้ไลบรารี `react-native-maps`
  * หน้าจอเลือกจุดบนแผนที่: `app/(sender)/map-picker.tsx`
  * หน้าจอติดตามสถานะและโฮมเพจ: `app/(sender)/index.tsx`, `app/(rider)/map.tsx`, และหน้าแสดงรายละเอียดออเดอร์
* **คำอธิบาย:**
  ในแอปได้ดึงคอมโพเนนต์ `<MapView>` มาแสดงผลควบคู่กับ `<Marker>` เพื่อให้ผู้ใช้อย่างคนส่งสามารถปักหมุดจุดรับ-ส่งของได้อย่างแม่นยำ และไรเดอร์สามารถใช้แผนที่ในการขับขี่ไปรับ-ส่งพัสดุได้

---

## 7. `database` (ระบบฐานข้อมูล)
การเก็บข้อมูลทั้งหมดของแอปไว้บน Cloud
* **ตำแหน่งในโค้ด:**
  * ไฟล์ `services/firebase.ts` ระบุการเชื่อมต่อฐานข้อมูลโดยคำสั่ง `getFirestore(app)`
  * บริการต่าง ๆ (Services) ในไฟล์เช่น `orderService.ts`, `authService.ts`
* **คำอธิบาย:**
  แอปพลิเคชันนี้ใช้ **Firebase Firestore** ในส่วนของ Database โดยฐานข้อมูลนี้รองรับการอัปเดตข้อมูลแบบ Real-time และใช้โครงสร้างแบบ NoSQL (Collections -> Documents) ตัวอย่างเช่น:
  * บันทึกข้อมูลผู้ใช้งาน (ตาราง `users`)
  * บันทึกข้อมูลการสั่งงาน (ตาราง `orders`)
  * บันทึกประวัติการแชท (ตาราง `chats`)
  โดยมีฟังก์ชันหลัก ๆ อย่างเช่น `addDoc` (เพิ่มข้อมูล), `updateDoc` (อัปเดตสถานะงาน), และ `onSnapshot` (ใช้ฟังการเปลี่ยนแปลงข้อมูลแบบสดๆ เพื่ออัปเดตหน้าจอโดยไม่ต้องรีเฟรชเอง)

---

## 8. `storage` (ระบบจัดเก็บไฟล์รูปภาพ)
การใช้พื้นที่จัดเก็บออนไลน์สำหรับการอัปโหลดและดาวน์โหลดไฟล์รูปภาพ
* **ตำแหน่งในโค้ด:**
  * ไฟล์ `services/firebase.ts` ในคำสั่ง `getStorage(app)`
  * ไฟล์ Service ชื่อ `services/storageService.ts` โดยใช้ฟังก์ชัน `uploadBytes` หรือ `uploadString` และ `getDownloadURL`
* **คำอธิบาย:**
  ระบบจัดเก็บไฟล์ใช้ **Firebase Storage** โดยเอาไว้ให้ผู้ใช้งาน หรือ ไรเดอร์ สามารถรับ-ส่งรูปภาพเข้ามาในแอปได้ เช่น:
  * การอัปโหลดรูปโปรไฟล์ (Profile Picture)
  * การถ่ายรูปพัสดุเป็นหลักฐานเพื่อแนบไปกับออเดอร์
  การทำงานคือจะเอาไฟล์รูปที่รับมาจากมือถือไปโยนใส่ Storage จากนั้นระบบจะสุ่มหรือแปลง URL ไฟล์เป็นลิงก์ (Download URL) ออกมาบันทึกไว้ใน Database (Firestore) เพื่อให้แอปนำไปโหลดแสดงผลได้
